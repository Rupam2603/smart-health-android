import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.smarthealth.app',
  appName: 'Smart Health Assistant',
  webDir: 'dist'
};

export default config;
